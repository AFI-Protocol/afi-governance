# AFI Current-Authority Re-Root & Deterministic-Golden Closure v0.1 (R1-GOV)

**Slot:** `AFI-GOV-CURRENT-AUTHORITY-REROOT-v0.1` (R1-GOV)
**Status:** **Accepted** owner decision — accepted by merge of afi-governance PR #29 on 2026-07-19 (merge commit `6195c8a`, merged unedited). It became authoritative on that owner merge. **Owner authorization is recorded by the owner instruction that commissioned this decision** (the Mission R1 "Current-Authority Re-Root & Golden Closure" mission, 2026-07-19, issued by the owner), under the convention FCP-GOV, DSC-GOV, and ATLAS-GOV record. **This decision authorizes a bounded set of governance-ledger re-root facts + the deletion of three superseded proof-of-concept-era instruments + a byte-identical fixture relocation + governed-schema/registry provenance re-pointing + token-free guard redesigns (enumerated in §0), and no runtime, scoring, UWR, evidence-shape, hash-law, persistence, provider, Gateway-routing, Atlas-graph, or economic change.**
**Date:** 2026-07-19
**Type:** Scoped protocol-development governance decision (current-authority re-root + superseded-instrument deletion authorization + deterministic-golden re-home + governed-schema/registry provenance re-point + bounded guard redesigns). Governance-ledger + delegated-schema/registry/fixture-path scope only. It re-records every still-live invariant currently rooted in a proof-of-concept-era instrument onto present AFI architecture, becomes the sole current authority for those invariants, and authorizes deletion of the superseded instruments after every current-facing citation is repointed. It creates **no** District, changes **no** District scope, alters **no** runtime behavior, and promotes **nothing** to production.
**Governance:** Subordinate to `AFI_DROID_CHARTER.v0.1.md` (canonical home `afi-config/codex/governance/droids/`; unamended and unaffected — the Charter carries no dependency on the instrument this decision retires) and `decisions/authority-districts-v0.1.md`. **This decision does not subordinate to the proof-of-concept-era District-framing annex; it re-roots and prospectively supersedes that annex in full (D-R1-1 … D-R1-4), carrying its still-live invariants forward onto present architecture, and — upon acceptance, and only after every current-facing citation is repointed — authorizes the annex's deletion from the active tree (D-R1-10).** **Consumes (does not re-decide)** `authority-districts-v0.1.md` (AUTH-GOV — the authority topology and District registry, whose two-District rule, tier model, and gates are honored), `district-surface-consolidation-v0.1.md` (DSC-GOV), `district-one-signal-evaluation-capability-v0.1.md` (D1CAP-GOV), `district-2-m2-ratification-v0.1.md` (D2R-GOV), `evidence-v3-provider-provenance-v0.1.md` (EV3-GOV), `factory-configurable-pipelines-v1.md` (FCP-GOV), `provider-byok-foundations-v0.1.md` (PBF-GOV), `five-lane-provider-runtime-v0.1.md` (FLPR-GOV), `research-institute-reference-services-v0.1.md` (INST-GOV), `district-api-atlas-foundation-v0.1.md` (ATLAS-GOV), `object-identity-v0.1.md` (OBJ-GOV), `lifecycle-v0.1.md` (LIFE-GOV), `persistence-v0.1.md`/`persistence-impl-v0.1.md` (MONGO-GOV/MONGO-IMPL), `uwr-profile-pin-v0.1.md`/`uwr-runtime-consumption-v0.1.md`, and `math-authority-v0.1.md`/`mint-formula-bt-86b-alignment-v0.1.md`. **Supersedes, prospectively and only as stated in D-R1-1 … D-R1-9 and nowhere else,** the enumerated clauses of the retired annex, the docs-homed M1 authorization report (the M0 planning report carries no live invariant and is deletion-only, not superseded as an authority source), and the specific golden-path / authority-source / functional-name / roster citations named in those clauses — all as governance-ledger fact; **no accepted decision file is rewritten.** Honors, and does not touch, the reserved `CHAIN-GOV`, `OBJ-GOV`, `LIFE-GOV`, and `MONGO-GOV` scopes. Where this decision conflicts with the Charter, the Charter wins.
**Evidence basis:** the Mission R1 read-only organization-wide census + authority-chain analysis (2026-07-19; a 24-agent primary-source pass over all 18 repositories; **input, not authority**; load-bearing findings restated inline in §1), verified against clean `origin/main` trees at the baseline heads: `afi-governance` @ `7c450dd`, `afi-config` @ `38ffb3e`, `afi-reactor` @ `51ae247`, `afi-core` @ `abd9778`, `afi-infra` @ `eac027a`, `afi-factory` @ `4191a28`, `afi-docs` @ `a23cf97`, `afi-protocol` @ `4d4af83`, `afi-gateway` @ `598acfe`, `afi-tiny-brains` @ `e8267d0`, `afi-math` @ `5247e41`, `afi-token` @ `0d3de3c` (and the eight further repositories confirmed clean of any residue). The workspace `reports/` directory is unversioned and is never cited as authority.
**Ledger slot:** `R1-GOV` — the current-authority re-root slot. It re-homes the still-live invariants that presently rest on the proof-of-concept-era District-framing annex and the docs-homed District-2 M1 authorization, so that the deletion of those instruments from the active tree leaves the accepted authority chain complete, present-tense, and independent of any retired instrument.

---

## 0. Scope — what this decision does and does not decide

**Decides (and only these 11):**

1. **D-R1-1** — District One current identity: **Signal Evaluation** (present-architecture description), re-rooting the functional-name binding formerly held by the retired annex.
2. **D-R1-2** — Standing protocol invariants (operation ≠ authority; no LLM substitution / no silent alteration of deterministic logic; no fabrication / no mock-as-production; **no-financial-truth**; **non-production ceiling**; owner-pinning of canonical choices) re-recorded here and made the current authority those invariants rest on.
3. **D-R1-3** — District-framing authority, the future-district candidate roster, the per-District separate-authorization rule, and the tier-2 standing formerly conferred on the retired annex are re-rooted onto this decision; every accepted-decision subordination reference to the retired annex now reads, as a governance-ledger fact, as subordination to R1-GOV.
4. **D-R1-4** — Current enrichment categories are **exactly `technical`, `pattern`, `sentiment`, `news`, `aiMl`**; no `social` category identity exists.
5. **D-R1-5** — District Two **M1** provenance-schema authorization (schema drafts + tests only; **no runtime wiring**; M2–M6 and other District work **not** authorized; per-phase separate authorization) is re-recorded here, absorbing the docs-homed authorization instrument; its current-facing citations repoint to this decision.
6. **D-R1-6** — The deterministic golden relocates **byte-identically** to a current-purpose path; the byte-identity + sha256 mandate is preserved and re-pinned to the new path across every standing condition.
7. **D-R1-7** — The afi-core / afi-config `compute-uwr-score` KAT provenance note is repointed to describe only the present invariant, under explicit proof that the numeric payload is byte-identical; the KAT's own self-integrity hash is re-pinned.
8. **D-R1-8** — The eight governed provenance-schema provenance notes repoint to this decision and propagate through the governed-schema closure (re-vendor, MANIFEST, generated files, checksum, commit-pins); governed-schema file hashes and closure pins may change, runtime/evidence/scoring hashes must not.
9. **D-R1-9** — The canonical Atlas registry authority citation is corrected to the re-rooted roster authority and re-vendored byte-identically; the Atlas shape, entity graph, counts, and contract identifier are unchanged.
10. **D-R1-10** — After acceptance and after every current-facing citation is repointed, the three superseded proof-of-concept-era instruments (the District-framing annex in `afi-config`; the District-2 M1 authorization report and the M0 planning report in `afi-docs`) are authorized for **deletion** from the active tree; Git history is the archive; no tombstone, alias, redirect, summary stub, or archive copy is retained.
11. **D-R1-11** — The current-facing negative-space guards that presently embed a discarded token are authorized to be redesigned into token-free positive-invariant checks that preserve their regression protection (an RC-7-class guardrail authorization).

**Does not decide (expressly):** any change to the one `GraphExecutor`, graph geometry, provider selection, the five category outputs, the enrichment join, analyst input, analyst score, UWR axes/score, decay, signal identity, the Evidence V3 shape, the record/replay hash laws, evidence persistence, the Atlas schema/entity-graph/registry semantics, Factory behavior, Gateway behavior, or any settlement/reward/mint/economic surface; any District creation, deletion, numbering change, or scope change; any production promotion or deployment (the non-production ceiling re-recorded in D-R1-2 stands; nothing is deployed); any new runtime API, endpoint, or route; any change to the numeric content of any KAT vector, golden value, or governed schema *shape*; any Charter amendment; and **any rewrite of an accepted decision file** (all supersession here is prospective governance-ledger fact; the only files edited in place are current-facing code, schemas, docs, registries, guards, and the fixture path — never an accepted `decisions/*` decision).

---

## 1. Verified evidence restated (input, not authority)

Re-verified at the pinned heads by direct reading of code, schemas, registries, reports, tests, and the accepted decision chain (the R1 census):

- **The retired District-framing annex is not dead paper — it is live tier-2 authority.** `afi-config/codex/governance/droids/AFI_DROID_PIPEHEAD_ADDENDUM.v0.1.md` (self-labeled "Status: Proposed") is cited as a superior instrument in the front matter of thirteen accepted decisions, pins concrete invariants (District framing, functional name, non-production ceiling, no-financial-truth, version-expansion roster) that six current decisions honor by section-citation, and is cited by the canonical Atlas registry as a live authority anchor. Its **entire** tier-2 standing derives from a single conferral: `authority-districts-v0.1.md` Part B item 2 records that the annex "is today cited by no accepted decision; this decision, upon owner merge, is the accepted decision that confers tier-2 standing on [its] District framing — and no more." Deleting the annex without re-rooting its live invariants would leave every one of those anchors pointing at a deleted authority. This decision re-roots them.
- **The docs-homed District-2 M1 authorization report authorizes a *live* artifact.** The report authorizes the afi-config `schemas/provenance/v1/` M1 schema family "for M1 only … No runtime wiring", which is **still consumed by the live Evidence V3 code** (`afi-reactor:src/evidence/provenance/schemaValidation.ts`). It is therefore a live authorization homed in a report rather than in the decisions ledger — not discarded implementation. Its M1-only invariant must be carried forward before the report is deleted.
- **The M0 planning report carries no live invariant.** It is self-labeled "a planning artifact, not an implementation authorization"; every governance citation of it resolves against the immutable pin `afi-docs @ 1f3f959`; no current-facing surface links it by live path. Git history at that pin is the archive.
- **The deterministic golden is byte-relocatable.** `afi-reactor/test/pipeheads/fixtures/golden.json` (1861 bytes, sha256 `312da1180b0bd418c03f595093516ebdc755ba81465a0b526ace43d002126e06`) is the last surviving proof-of-concept-named path in the active tree; its directory otherwise contains no other file after the DSC-GOV D-DSC-2 deletions. A path move preserves its bytes and sha256 exactly; its sole functional consumer is `test/evidence/provenance/builders.test.ts`, and the current repo convention co-locates fixtures with the consuming suite under `test/evidence/provenance/fixtures/`.
- **Runtime residue is already absent from live code.** The current pipeline is `froggy-trend-pullback v1.3.0`; the sole evidence contract is `afi.scored-signal-evidence.v3`; the five governed categories are `technical`, `pattern`, `sentiment`, `news`, `aiMl` (no sixth `social` category); the one `GraphExecutor` lives at `afi-reactor:src/pipeline/executor.ts` (sole construction site `src/config/runtimeComposition.ts`). The remaining residue is confined to the three superseded instruments, their citations, the golden path, the governed provenance-schema provenance notes, the Atlas authority citation, and a set of negative-space guards that embed the token they ban.

---

## 2. D-R1-1 — District One current identity: Signal Evaluation

**Decision.** District One is the **Signal Evaluation** capability and authority domain, described only through present architecture: canonical signal input → five provider-selected enrichment categories (`technical`, `pattern`, `sentiment`, `news`, `aiMl`) composed by the one `GraphExecutor` → deterministic join → the analyst/scorer/UWR seam → handoff to District Two (Evidence and Provenance / `afi.scored-signal-evidence.v3`) → hash-verified persistence. Its functional name is **Signal Evaluation**. This re-records, onto this decision, the District-One functional identity formerly bound to the retired annex's §12 term; it **prospectively supersedes** the "Functional name (Addendum §12)" and "Authority source — Addendum §9/§12/§13" registry cells of `authority-districts-v0.1.md` Part D and `district-one-signal-evaluation-capability-v0.1.md` (the functional-name registry row), which now read as "Functional name: Signal Evaluation" and "Authority source: GOVERNED — R1-GOV (District-framing re-root) + D1CAP-GOV", as a governance-ledger fact and without rewriting those files. This aligns the functional name with the canonical Atlas (`"name": "Signal Evaluation"`), D1CAP-GOV D-D1CAP-1/2, and DSC-GOV D-DSC-7.

**Scope-guard.** Identity re-record only; creates or changes no District, no scope, no runtime behavior, and no Atlas entity — it re-homes an existing functional-name binding onto current authority.

## 3. D-R1-2 — Standing protocol invariants re-rooted onto current authority

**Decision.** The standing protocol invariants formerly rooted in the retired annex are re-recorded here as the current authority they rest on. Each is carried forward under present-architecture terms; the substance of each is already enforced by the named current decisions, so this clause re-homes the **authority**, not the enforcement, and every current decision's citation of "the Addendum §9/§13 non-production ceiling" or "the Addendum §14 no-financial-truth rule" now reads as a citation of the corresponding clause below:

1. **Operation ≠ authority.** Agents may operate AFI's evaluation and evidence machinery — the one `GraphExecutor`, its enrichment categories, its providers, and the surfaces around them — but the deterministic scoring/validation/audit logic (the analyst/scorer/UWR seam and the `afi-math` kernels it invokes) is the sole source of protocol truth. Operating a surface confers no authority over it. Humans and governance decide high-risk protocol changes; any ambiguity defaults to not authorized. *(Enforced by AUTH-GOV Part A/B/F and INST-GOV; unchanged.)*
2. **No LLM substitution; no silent alteration of deterministic logic.** An agent may invoke the deterministic scoring/validation/audit path over a `GraphExecutor` result but may not replace it with LLM judgment, nor silently alter, replace, or reinterpret it. No agent may silently change UWR weights or profile values, scoring law, reputation math, reward/emissions math, settlement logic, or the Evidence V3 shape; each requires a new governed decision and version. *(Enforced by `uwr-profile-pin`/`uwr-runtime-consumption`, `math-authority`, EV3-GOV, and FCP-GOV D-FCP-3's no-silent-mutation rule; unchanged.)*
3. **No fabrication; no mock-as-production; no hidden categories.** No agent may fabricate market data or Evidence V3 provenance, silently substitute a mock/placeholder provider for a governed one, present demo/mock output as production truth, or conceal an unrun or failed enrichment category behind a generated explanation. *(Enforced by FCP-GOV D-FCP-8 / FLPR-GOV no-demo-fallback and the organization-wide non-production posture; unchanged.)*
4. **No-financial-truth.** Agents may operate the pipeline, its enrichment categories, and the Evidence V3 / provenance surfaces and may invoke the deterministic logic — but agents may not become the source of financial truth. AFI's trust-critical outputs (scores, validation decisions, evidence, and any future settlement/reward/reputation values) must remain deterministic, auditable, replayable, and governed by explicit protocol rules. **This is the standing invariant every District decision honors; it is now rooted here.**
5. **Non-production ceiling.** The entire effort remains non-production: nothing is deployed. No agent or mission may perform production deployment, live trading, minting, token settlement, treasury operations, or reward distribution, touch production keys or introduce live external dependencies, or change core scoring/reputation/emissions math — absent an explicit, separately-governed owner authorization. **This ceiling stands until a future governed decision lifts it, and is now rooted here.**
6. **Owner-pinning of canonical choices.** Before implementation, the owner must explicitly pin the canonical choices — the `GraphExecutor` composition/pipeline path, the analyst/scorer/UWR profile, and which outputs are canonical, provisional, or demo-only. Agents may apply pinned choices but may not select a canonical scoring, evidence, or composition path silently; each such selection is a separately-authorized act. *(Enforced by AUTH-GOV Part F.3 and PBF-GOV; unchanged.)*

**Scope-guard.** Authority re-home only; re-records invariants already in force and already enforced by the named decisions. It grants no new power, changes no runtime behavior, and lifts no ceiling.

## 4. D-R1-3 — District framing, roster, per-District authorization, and tier-2 standing re-rooted

**Decision.**
1. **District framing.** Districts are governance-gated, separately-authorized capability/authority domains, named by function. Exactly two active Districts exist — District One (Signal Evaluation) and District Two (Evidence and Provenance) — and this decision creates no other. This re-records the District-framing definition formerly attributed to the retired annex (`authority-districts-v0.1.md` Part D line 88) onto this decision.
2. **Future-district candidate roster.** Beyond the two active Districts, the following capability domains are recorded as **unnumbered, uncreated candidates**, not active Districts: reputation, contracts, settlement-readiness, monitoring, documentation, and external-agent interfaces (the economic/settlement/reward/reputation domain is the reserved `CHAIN-GOV` candidate; provenance is already realized as District Two). No District 0, District 3, or higher is created or implied. This re-records the roster that `authority-districts-v0.1.md` Part D.2 attributes to the retired annex's §13, onto this decision.
3. **Per-District separate authorization.** Creation of a new District, or a material scope change to an existing District, requires its own governance decision or owner-recorded authorization; a broad mission prompt is never sufficient. *(Consistent with AUTH-GOV Part E.1 and Part F.)*
4. **Tier-2 re-root (non-circularity).** `authority-districts-v0.1.md` Part B item 2 conferred tier-2 standing on the retired annex's District framing — the annex's *entire* accepted-governance standing. Upon owner merge, **this decision (a tier-1 accepted decision in `decisions/`) carries the District-framing authority the annex held**, so that authority now lives at tier 1 in this file rather than at tier 2 in a deleted instrument. Prospectively and as governance-ledger fact, this **supersedes** `authority-districts-v0.1.md` Part B item 2's annex clause, its Part D authority-source cells, its Part D.2 roster attribution, and its Part G "unchanged and not amended" note **as they pertain to the retired annex only** — the Charter clauses of each are untouched — and **every accepted-decision front-matter line that reads "Subordinate to … `AFI_DROID_PIPEHEAD_ADDENDUM.v0.1.md`" now reads as subordination to `R1-GOV`,** which carries that annex's District-framing scope forward. No accepted decision file is rewritten; those files retain their original bytes as governance-ledger history, and this decision is their live re-root.

**Scope-guard.** Authority re-root and prospective supersession only; creates, deletes, renames, and re-scopes no District; the two-District rule and AUTH-GOV's tier model, gates, and agent limits are unchanged.

## 5. D-R1-4 — Current enrichment categories

**Decision.** AFI evaluates a submitted signal across **exactly five** first-class enrichment categories — `technical`, `pattern`, `sentiment`, `news`, `aiMl` — composed by the one `GraphExecutor`, combined by the deterministic join, and handed to the analyst/scorer/UWR seam. The composition may run categories in parallel, sequentially, or conditionally but must not collapse into a single linear scoring step, and no category may be silently dropped or hidden. **No `social` category identity exists**; the sentiment surface the retired annex called "Social" is the current `sentiment` category (FCP-GOV D-FCP-1; DSC-GOV D-DSC-7). This re-records the five-category namespace as current, superseding the retired annex's five-lane design description.

**Scope-guard.** Category-set re-record; changes no schema, no node, no provider selection, and no output — it states the existing governed namespace and retires the obsolete category name.

## 6. D-R1-5 — District Two M1 provenance-schema authorization re-rooted

**Decision.** The District Two **M1** authorization — afi-config provenance-schema drafts and tests only; **"No runtime wiring"**; it does **not** authorize M2–M6 or any other District work; and every implementation phase after M1 requires its own scoped authorization — is re-recorded here as current authority, absorbing the docs-homed authorization report. The M1 schema family it authorizes (`afi-config/schemas/provenance/v1/`) is live and unchanged; only the *home of its authorization* moves from a report into this decision. Prospectively and as governance-ledger fact, this **supersedes** the docs-homed authorization report as an authority source and re-roots onto this decision every citation of it in `authority-districts-v0.1.md` (Part D District-2 authority/scope cells and Part D.1) and `district-2-m2-ratification-v0.1.md` (its M1-instrument references). Every **current-facing** citation of that report — the eight governed provenance-schema provenance notes (D-R1-8), the afi-config provenance README, `AFI_CONFIG_OVERVIEW.md`, the provenance-schema-validation test header, the afi-docs `AFI_Full_Architecture.md` District-2 line, and the afi-protocol `README.md` link — repoints to this decision. After all such repointing, the report is authorized for deletion (D-R1-10). The M2 authorization-gap disposition (`authority-districts-v0.1.md` D.1 / `district-2-m2-ratification-v0.1.md`) is unchanged by this decision.

**Scope-guard.** Authorization re-home only; the M1 schema shapes, validators, and District-2 scope are unchanged; no phase beyond M1 is authorized and no runtime is wired.

## 7. D-R1-6 — Deterministic golden re-home (byte-identical) and governed-path re-pin

**Decision.** The deterministic golden relocates from `afi-reactor/test/pipeheads/fixtures/golden.json` to the current-purpose path **`afi-reactor/test/evidence/provenance/fixtures/golden.json`** (the current per-suite fixture convention, co-located with its sole functional consumer `test/evidence/provenance/builders.test.ts`), by a **path move that preserves the file's bytes and its sha256 `312da118…2126e06` exactly**. No value in the file changes; no golden is regenerated or re-hashed. The now-empty `test/pipeheads/` tree is removed. Prospectively and as governance-ledger fact — **byte-identity and the sha256 mandate preserved, only the path binding moved** — this **supersedes** the "The governed golden stays at its governed path" byte-identity-at-path term of DSC-GOV D-DSC-3, the DSC-GOV D-DSC-8 guardrail path binding and its §1 standing-golden condition, the golden-path naming in `uwr-runtime-consumption-v0.1.md` (`:40` sha + "must never change" / `:175` "byte-identical" / `:146` ban-scope), `uwr-profile-pin-v0.1.md` (`:161` UP-11 / `:33` / `:103`), and the `afi-config/schemas/uwr-profile/v0/README.md:26` mint-eligibility gate — each of which now names the new path with the same sha256. It authorizes editing the golden's sole functional consumer (`afi-reactor/test/evidence/provenance/builders.test.ts` `GOLDEN_PATH`) and the DSC-GOV D-DSC-8 CI guardrail (`afi-reactor/test/guardrails/districtSurfaceConsolidation.test.ts` `GOLDEN_RELATIVE_PATH`) to the new path (an RC-7-class guardrail amendment), the mint-eligibility gate remaining intact.

**Scope-guard.** Path relocation + path re-pin only; the golden's bytes, sha256, the mint-eligibility gate, and every value it anchors are unchanged. If the relocation cannot be completed byte-identically, it must not merge (DSC-GOV D-DSC-3's byte-stability scope-guard, carried forward).

## 8. D-R1-7 — KAT provenance re-point under explicit proof

**Decision.** The `compute-uwr-score` KAT vector `uwr-score-d2m2-golden-anchor` carries a `description`/provenance note that names the old golden path. That note is repointed to describe only the present invariant — e.g. *"Canonical Signal Evaluation deterministic golden used to verify current UWR/scoring behavior; byte-stability of the golden is an acceptance criterion (UP-5, UP-11)."* — with no old path and no retired-implementation name. Because the note is a field of a self-integrity-pinned KAT, editing it changes the KAT file's **own** sha256 (`ff2f639…`). This is authorized **under explicit proof** that the KAT's numeric payload is byte-identical (every vector value, including `uwrScore = 0.1875 = mean(0.15, 0, 0.2, 0.4)` and the UP-5/UP-11 acceptance criterion, is unchanged; only the human-readable description metadata changes). The re-pin propagates from the afi-config source-of-truth (`kats/uwr-profile/v0/compute-uwr-score.kat.json`) to the byte-identical afi-core vendored copy (`validators/__tests__/kats/compute-uwr-score.kat.json`), and the recorded `PINNED_SHA256` in `afi-core/validators/__tests__/computeUwrScore.kat.test.ts` and `validators/__tests__/kats/README.md` is re-pinned to the new value, with the change recorded in the afi-config KAT change-control note. In the same pass, the consuming test's old-path comment and the now-stale vendoring-provenance header (the recorded content-commit/blob of the vendored KAT) in `computeUwrScore.kat.test.ts` are refreshed to the post-repoint state, so no old golden path and no stale blob pin survive. No governed KAT hash is altered silently.

**Scope-guard.** Description-metadata re-point + consequent self-integrity re-pin only, under proof of numeric byte-identity; no KAT vector value, no `uwrScore`, no scorer/UWR behavior changes.

## 9. D-R1-8 — Governed provenance-schema provenance re-point and closure propagation

**Decision.** The eight governed provenance-schema provenance notes that cite the docs-homed District-2 M1 authorization report by its decision identifier (`afi-config/schemas/provenance/v1/*.schema.json` `$comment`/`x-afiDoctrineRefs`, the eight artifact-kind schemas) are repointed to cite **this decision (R1-GOV)** as the current M1 authorization (D-R1-5). Because the governed provenance schemas are vendored into the governed-schema closure, this change propagates through the **normal closure process**: byte-identical re-vendor of the affected schemas into `afi-factory` (1 schema), `afi-infra` (5 schemas), and `afi-reactor` (1 schema), regeneration of any generated `.ts` mirror, re-pin of each vendored `MANIFEST.json` per-file sha256 + `afiConfigCommit`, checksum re-derivation (`b6057d16…`-class composition-ref checks re-run), and the afi-config commit-pin bumps in the consumers. **Governed-schema file hashes and closure pins may change** because the provenance notes intentionally change; **the runtime, scorer, UWR, Evidence V3, record/replay, persistence, and composition hashes must remain byte-unchanged** (they do not commit these `$comment` bytes — verified by re-running the affected repos' hash-law and byte-equivalence gates). No governed schema *shape* or *validation behavior* changes.

**Scope-guard.** Provenance-note re-point + mechanical closure propagation only; schema shape, validators, and every runtime/evidence hash are unchanged; the change is confined to documentary `$comment`/`x-afiDoctrineRefs` text and the file-integrity pins that commit it.

## 10. D-R1-9 — Atlas registry authority-citation correction

**Decision.** In the canonical Atlas registry `afi-config/registries/afi-protocol-atlas.v1.json`, the reserved `chain-settlement` district's `authorityRefs` entry that names the retired annex's §13 roster, and the adjacent `purpose`-field mention of that annex, are corrected to cite the re-rooted roster authority (`authority-districts-v0.1` Part D.2 and this decision's D-R1-3 roster) — preserving the §13-roster *semantics* while dropping the retired-instrument name and leaving no dangling reference. The registry's shape, entity graph, District/capability/interface counts, referential integrity, `descriptive/read-only` semantics, and the `afi.protocol-atlas.v1` contract identifier are **unchanged** (D-ATLAS-9 and the §11 Atlas-invariance requirement are preserved; the `authorityRefs` array stays ≥ `minItems:1`). The corrected registry is re-vendored **byte-identically** into `afi-docs/atlas/afi-protocol-atlas.v1.json`, and `afi-docs/atlas/PROVENANCE.txt` is re-pinned (sha256 of the vendored bytes + the afi-config commit that lands the correction); `scripts/check_atlas_summary.py` re-verifies. This is a governed content correction on an invariant surface citing a retired instrument, which only this decision authorizes.

**Scope-guard.** One authority-citation correction + byte-identical re-vendor + provenance re-pin; no District, capability, interface, route, count, or contract-shape change to the Atlas.

## 11. D-R1-10 — Superseded-instrument deletion authorization

**Decision.** After this decision is accepted (owner merge) **and** after every current-facing citation enumerated in D-R1-5 … D-R1-9 has been repointed, the following three superseded proof-of-concept-era instruments are authorized for **deletion from the active tree**:
1. the District-framing annex — `afi-config/codex/governance/droids/AFI_DROID_PIPEHEAD_ADDENDUM.v0.1.md` (its live invariants re-rooted by D-R1-1 … D-R1-4);
2. the District-2 M1 authorization report — `afi-docs/reports/district-2-d17-implementation-authorization.md` (its live invariant re-rooted by D-R1-5);
3. the M0 planning report — `afi-docs/reports/district-2-m0-canonical-data-boundary-and-hash-doctrine.md` (carries no live invariant; cited only at the immutable pin `afi-docs @ 1f3f959`).

**Git history is the archive.** No tombstone, redirect, alias, summary stub, "this file was removed" notice, or archive copy is retained. Deletion may proceed only after the repoint proofs of D-R1-5 … D-R1-9 are complete and a no-dangling-current-reference check passes; the accepted-decision ledger's historical mentions of these instruments are governance-ledger history (resolved at the ledger level by this decision's re-root), not dangling current references, and are not touched.

**Scope-guard.** Deletion authorization only, gated on prior repointing; no accepted decision file is deleted or rewritten; the ordering (authority re-root and citation repoint precede deletion) is mandatory.

## 12. D-R1-11 — Token-free negative-space guards

**Decision.** The current-facing negative-space guards that presently embed a discarded token in their own source are authorized to be redesigned into **token-free positive-invariant** checks that preserve their regression protection, using only current directory-structure manifests, the exact five-category set (`technical/pattern/sentiment/news/aiMl`), current contract/registry set-equality, sole-`GraphExecutor` / sole-`.v3`-evidence-surface uniqueness, escaped-dot structural version capture, absence-of-undeclared-path, and (where a literal sweep is genuinely required) needles assembled from fragments at runtime. No redesigned guard's source may contain a discarded literal, so none needs a self-exemption. This authorization (an RC-7-class guardrail-amendment act) covers: the afi-reactor `no-stale-current-terminology` and `districtSurfaceConsolidation` guards (and the `no-legacy-reactor-vault` stale `.v1` comment and the `jest.config.js` registration comments); the afi-docs `check_architecture_doc.py` term/path bans and the `AFI_Full_Architecture.md` disallowed-alias sentence and the `check_atlas_summary.py` comment; the afi-config `atlas-registry-validation` residue ban-list; the afi-factory `current-state-residue` guard; and the afi-core `no-legacy-dag-path` guard. Each redesign must keep its suite green and must still fail if the retired surface returns.

**Scope-guard.** Guard-redesign authorization only; each guard's enforcement is preserved or strengthened; no guard is deleted outright unless its invariant is fully carried by a positive check, and no test is weakened or skipped.

---

## Explicit non-authorizations

This decision does **not** authorize: any change to the one `GraphExecutor`, graph geometry, runtime composition, category nodes, provider selection/adapters, the five category outputs, the enrichment join, analyst input/score, UWR axes/score/decay, signal identity, the Evidence V3 shape, the record/replay hash laws, evidence persistence, or the composition/manifest/analyst-config hashes; any change to the numeric content of any KAT vector, any golden value, or any governed-schema *shape* or *validation behavior*; any change to the Atlas schema, entity graph, counts, or `afi.protocol-atlas.v1` contract semantics; any Factory, Gateway, settlement, reward, mint, treasury, or on-chain change (CHAIN-GOV reserved); any District creation, deletion, numbering change, or scope change; any production promotion, deployment, or secret provisioning; any object-identity, lifecycle, or persistence-store change (OBJ-GOV/LIFE-GOV/MONGO-GOV reserved); any Charter amendment; and **any rewrite of an accepted decision file** (all supersession here is prospective governance-ledger fact — the accepted `decisions/*` files retain their original bytes; only current-facing code, schemas, docs, registries, guards, and the fixture path are edited in place, and only three superseded non-decision instruments are deleted).

---

## Supersessions and interactions (every prior decision walked)

- **`authority-districts-v0.1.md` (AUTH-GOV)** — consumed and honored; its two-District rule, Part B tier model, Part E gates, and Part F agent limits are unchanged. Prospectively superseded **only** as to the retired annex: Part B item 2's annex tier-2 conferral, the Part D "Functional name (Addendum §12)" / "Authority source — Addendum §9/§12/§13" cells, the Part D.2 "§13 roster" attribution, and the Part G "annex unchanged and not amended" note are re-rooted onto this decision (D-R1-1/3); its Part D District-2 authority/scope citations of the docs-homed M1 report re-root onto D-R1-5. The Charter clauses of each are untouched. The file is not rewritten.
- **`district-surface-consolidation-v0.1.md` (DSC-GOV)** — consumed; its sole-executor record (D-DSC-1), the District-Two law relocation (D-DSC-3), the D1-implementation retirement, and the five-category terminology (D-DSC-7) stand. Prospectively superseded **only** as to the golden path: D-DSC-3's "governed golden stays at its current literal path" term and the D-DSC-8 guardrail path binding + §1 standing-golden condition are re-pinned to the new path (D-R1-6), byte-identity and sha256 preserved. The D1-fixture deletion list (which expressly excluded the golden) is now complete once the golden relocates.
- **`district-one-signal-evaluation-capability-v0.1.md` (D1CAP-GOV)** — consumed; District One identity/boundary/exclusions stand. Its functional-name registry row (bound to the annex §12 term) is re-rooted by D-R1-1; its five-category boundary is re-stated by D-R1-4. No scope change.
- **`district-2-m2-ratification-v0.1.md` (D2R-GOV)** — consumed; the M2 disposition is unchanged. Its references to the docs-homed M1 authorization report re-root onto D-R1-5 (ledger fact); the report's deletion (D-R1-10) does not disturb D2R's pinned evidence basis (git history at the report's pinned commit is the archive).
- **`evidence-v3-provider-provenance-v0.1.md` (EV3-GOV)** — consumed and honored; `afi.scored-signal-evidence.v3` is the sole evidence contract and is unchanged. Its "the Addendum §13 ceiling / §14 rule are honored" front-matter citation now reads as a citation of D-R1-2 (ledger fact). Its forward-only-deletion precedent (D-EV3-8) is the model for D-R1-10.
- **`factory-configurable-pipelines-v1.md` (FCP-GOV)** — consumed; the five-category namespace (D-FCP-1), composition model (D-FCP-2), no-demo-fallback (D-FCP-8), and no-silent-mutation (D-FCP-3) are the current enforcement of the invariants D-R1-2/D-R1-4 re-home. No FCP clause is superseded.
- **`five-lane-provider-runtime-v0.1.md` (FLPR-GOV)** — consumed; the five live provider-backed lanes stand. Its "the Addendum §13/§14 are honored" front-matter citation now reads as a citation of D-R1-2 (ledger fact). No clause superseded.
- **`provider-byok-foundations-v0.1.md` (PBF-GOV)** — consumed; it carries **no** Addendum citation (its Governance line subordinates only to the Charter and AUTH-GOV), so there is nothing to re-root. The provider socket and the three non-secret objects stand; no clause superseded.
- **`research-institute-reference-services-v0.1.md` (INST-GOV)** — consumed; the operation-≠-authority doctrine it re-uses is now rooted in D-R1-2(1). No clause superseded.
- **`district-api-atlas-foundation-v0.1.md` (ATLAS-GOV)** — consumed and honored; D-ATLAS-9's canonical-registry delegation and the Atlas shape/graph/counts are unchanged. The single `authorityRefs`/`purpose` citation of the retired annex is corrected to the re-rooted roster authority (D-R1-9); its "Addendum §9/§13/§14 honored" front-matter citation now reads as a citation of D-R1-2/D-R1-3 (ledger fact). ATLAS-GOV D-ATLAS-12's forward-only current-facing-surface rule is the doctrine D-R1-10 executes for the three deleted instruments.
- **`object-identity-v0.1.md` (OBJ-GOV)** / **`lifecycle-v0.1.md` (LIFE-GOV)** / **`persistence-v0.1.md`+`persistence-impl-v0.1.md` (MONGO-GOV/MONGO-IMPL)** — untouched; reserved scopes honored; their "Subordinate to … annex" front-matter lines are re-rooted onto R1-GOV as ledger fact (D-R1-3(4)) with no other change.
- **`uwr-profile-pin-v0.1.md` / `uwr-runtime-consumption-v0.1.md`** — consumed; all scorer/UWR pins in force. The golden-path standing conditions (UP-11 `:161`/`:33`/`:103`; RC `:40`/`:175`/`:146`) are re-pinned to the new path (D-R1-6), byte-identity + sha256 preserved; the RC-7 guardrail-amendment pattern authorizes the guard edits (D-R1-6, D-R1-11). No profile value or UWR pin changes.
- **`math-authority-v0.1.md` / `mint-formula-bt-86b-alignment-v0.1.md`** — untouched; afi-math remains sole canonical math authority; no economic surface is decided (CHAIN-GOV reserved).
- **Reserved `CHAIN-GOV`, `OBJ-GOV`, `LIFE-GOV`, `MONGO-GOV`** — honored and untouched; the reserved economic/settlement domain remains a reserved capability-domain candidate (re-rooted roster, D-R1-3(2)/D-R1-9), never an active District.
- **The retired District-framing annex, the docs-homed M1 authorization report, and the M0 planning report** — the annex's and the M1 report's still-live invariants are re-recorded here (D-R1-1 … D-R1-5); the M0 planning report carries no live invariant (cited only at the immutable pin `afi-docs @ 1f3f959`). After every current-facing citation is repointed, all three are deleted from the active tree (D-R1-10); Git history is their archive.

---

**Accepted owner decision (afi-governance PR #29, merge commit `6195c8a`, merged unedited). Authoritative upon owner merge. It re-roots the still-live invariants of three superseded proof-of-concept-era instruments onto present AFI architecture, authorizes their deletion after every current-facing citation is repointed, relocates the deterministic golden byte-identically, and re-points governed-schema/registry provenance — and authorizes no runtime, scoring, UWR, evidence-shape, hash-law, persistence, Atlas-graph, District, production, or economic change.**
